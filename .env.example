# Databricks workspace URL (used by CLI and DABs — no hardcoded URLs in databricks.yml)
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com

# Databricks CLI profile from ~/.databrickscfg (for OAuth or PAT auth)
DATABRICKS_CONFIG_PROFILE=DEFAULT
